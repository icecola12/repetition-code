reform: |-
  Problem: {promblem}

  Below is a earlier chat history on this problem. This trajectory shows signs of reverse engineering and hint leakage, Your task is to rewrite the data into a reasoning content and answer of SFT training example that only get the promblem as the user message:

  === Chat history Begin ===
  {chat_history}
  === Chat history End ===

  Produce only valid JSON with exactly two fields: 
  {
    "reasoning_content": "The cot should be self-contained and present the full trail-and-error process, a multi-branch reasoning trace. Important, The reasoning_content should have high information density and sufficient length for SFT training. It should generally be no shorter than the usable reasoning in the input after cleaning, unless the original problem is extremely simple. If the input reasoning is short, incomplete, or contaminated by leaked information, reconstruct a fuller self-contained derivation from scratch.",
    "content": "The final answer"
  }
  